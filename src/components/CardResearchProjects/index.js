import classNames from 'classnames/bind';
import styles from './CardResearchProjects.module.scss';

const cx = classNames.bind(styles);

function CardResearchProjects() {
    return <div className={cx('wrapper')}></div>;
}

export default CardResearchProjects;
